import {NavBar, ViewAuth} from "../components";

export const Evidences = () => {

    return (
        <div>
            <NavBar/>
            <ViewAuth/>
            Evidencias
        </div>
    );
}