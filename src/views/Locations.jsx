import {NavBar, ViewAuth} from "../components";

export const Locations = () => {

    return (
        <div>
            <NavBar/>
            <ViewAuth/>
            Ubicaciones
        </div>
    );
}